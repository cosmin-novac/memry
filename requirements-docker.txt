# Runtime container dependencies. Keep in sync with pyproject.toml; the test
# suite enforces this so Docker never reuses an incomplete dependency layer.
mcp>=1.28.1,<2
anyio>=4.0
pydantic>=2.7
httpx>=0.27
numpy>=1.26
starlette>=0.37
uvicorn>=0.30
python-multipart>=0.0.9
anthropic>=0.40
